upsc ups0@localhost  # nut test check server response
upscmd -l ups0@localhost  # nut list commands supported by ups
upsmon -c fsd  # nut perform system shutdown
upsdrvctl -t shutdown  # nut test system shutdown, output driver actions to the console without actually executing them
